# No unit tests yet - need to develop these!


## make tests to check that all constraints are correct length


# iter = 0
#
# for(i in production_constraint) {
#
#   iter <- iter + 1
#
#   if((length(i[[1]]) != length(i[[2]])) | (length(i[[3]]) != length(i[[4]]))) {
#
#     print(iter)
#     print(i)
#   }
#
# }
